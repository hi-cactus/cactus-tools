interface Node {
    id: string | number | symbol;
    label: any;
    children?: Node[];
}

export const getSibings = () => {
};

export const toArray = () => {
};

export const toTree = () => {
};

export const getNodeById = () => {
};
